node_modules/

# raw upstream dumps: large (~6MB) and regenerated by `npm run fetch-data`;
# only the derived data/index/*.json is committed.
data/raw/

# generated .build output isn't source (but keep real fixtures under test/)
/*.build
!package.json
!package-lock.json
!test/fixtures/*.build
