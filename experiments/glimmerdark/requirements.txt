# Simulator: standard library only (Python 3.10+).
# Print generators, Designer's Notes and verification:
reportlab>=4.0
matplotlib>=3.8
numpy>=1.26
fonttools>=4.40
pymupdf>=1.24
pytest>=8.0
# Optional, only for `minis/meshy_batch.py --stl`:
# trimesh>=4.0
